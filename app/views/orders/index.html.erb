<p id="notice"><%= notice %></p>
<h3 class="text-center">Pavel N.<br/>
  Here is my version of the job. I'm done this with the JS and Ruby on Rails. Double check: on the client and server.</h3>
<p>&nbsp;</p>
<div class="text-center">
  <h1>Light order form, for Intersog :-)</h1>

  <table>
    <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @orders.each do |order| %>
      <!--  <tr>
          <td><%#= link_to 'Show', order %></td>
          <td><%#= link_to 'Edit', edit_order_path(order) %></td>
          <td><%#= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>-->
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Order', new_order_path, class: "btn btn-info btn-wide btn-simple" %>
</div>